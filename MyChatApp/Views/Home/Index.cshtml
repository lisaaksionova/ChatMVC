@model IEnumerable<Chat>

<div class="chat-body">
    @foreach (var chat in Model)
    {
        <a class="btn btn-info" style="margin: 20px 70px 20px 70px;" asp-controller="Home" asp-action="JoinRoom" asp-route-id="@chat.Id">
            @chat.Name
        </a>
    }
</div>